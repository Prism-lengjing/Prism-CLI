# {{projectName}}

由 Prism-CLI 创建的 Express + TypeScript 项目。

## 安装依赖

- npm: `npm install`
- pnpm: `pnpm install`
- yarn: `yarn`
- bun: `bun install`

## 本地开发

- npm: `npm run dev`
- pnpm: `pnpm dev`
- yarn: `yarn dev`
- bun: `bun run dev`

## 构建与运行

- 构建：
  - npm: `npm run build`
  - pnpm: `pnpm build`
  - yarn: `yarn build`
  - bun: `bun run build`
- 运行：
  - npm: `npm run start`
  - pnpm: `pnpm start`
  - yarn: `yarn start`
  - bun: `bun run start`

作者：{{author}}  |  许可证：{{license}}